// Fixed background. Pure CSS radial gradients — NO blur filters, NO animation,
// so it paints once and costs nothing to scroll over.
export default function Aurora() {
  return <div className="aurora" aria-hidden="true" />
}
